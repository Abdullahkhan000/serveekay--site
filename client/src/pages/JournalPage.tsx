import { ArrowUpRight } from 'lucide-react';
import { Link } from 'wouter';
import { Eyebrow, PageFrame, Reveal } from '../components/SiteShell';

const notes = [{ tag:'Perspective', title:'The best interfaces leave room for people to think.', text:'A short note on restraint, hierarchy, and why clarity is a competitive advantage.' }, { tag:'Process', title:'What a design system should actually do for you.', text:'Beyond tokens and components: building a shared language that helps teams move with confidence.' }, { tag:'Practice', title:'The first draft is a conversation starter.', text:'Why showing unfinished work early can create better decisions and stronger partnerships.' }];
export default function JournalPage() { return <PageFrame><main className="editorial-page journal-page"><section className="journal-hero"><div className="container"><Eyebrow>Journal / notes from the work</Eyebrow><Reveal><h1>Thoughts on making<br /><em>things matter.</em></h1></Reveal><p>Occasional notes about product, brand, culture, and the details that make digital experiences feel inevitable.</p></div></section><section className="journal-list section-pad"><div className="container">{notes.map((note, index) => <Link href="/lets-work-together" className="journal-card" key={note.title}><span>0{index + 1} — {note.tag}</span><h2>{note.title}</h2><p>{note.text}</p><ArrowUpRight size={22} /></Link>)}</div></section></main></PageFrame>; }
