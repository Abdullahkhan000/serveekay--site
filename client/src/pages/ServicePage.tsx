import { ArrowLeft } from 'lucide-react';import { Link,useRoute } from 'wouter';import { Eyebrow,PageFrame,Reveal } from '../components/SiteShell';import { getService,services } from '../data/site';
export default function ServicePage(){const [,p]=useRoute('/service/:slug');const item=getService(p?.slug)||services[0];return <PageFrame><main className="route-new"><div className="mark-wrap route-back"><Link href="/#practice"><ArrowLeft size={15}/> Practice</Link></div><section className="mark-wrap route-hero"><Reveal><Eyebrow>{item.number} / Practice</Eyebrow><h1>{item.title}<i>.</i></h1><p>{item.description}</p></Reveal><Reveal><img src={item.image} alt={item.title}/></Reveal></section><section className="mark-wrap route-story"><div><Eyebrow>How I help</Eyebrow><h2>Make the next version feel inevitable.</h2></div><div><p>{item.description}</p>{item.deliverables.map(x=><p className="story-point" key={x}>{x}</p>)}</div></section></main></PageFrame>}
